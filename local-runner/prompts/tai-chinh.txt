Run the daily affiliate scout for topic Finance ('tai-chinh') with the agent team of this repository (Affiliate-Research, branch master). This is an unattended scheduled run on the user's own computer.

1. Sync first: git checkout master, then git pull origin master.
2. Use the skill affiliate-orchestrator in mode 'scout' with topic slug 'tai-chinh-doi'. Apply exactly the criteria of topic 'tai-chinh' from the skill affiliate-scout: finance product launched between 2025-01-01 and today, with an affiliate/CPA program open to marketers running Google Ads, consumer-facing, and no ban on bidding on the brand name. Watch for scam signals (hidden WHOIS, paid PR, token presales). If a source mentions legal or compliance requirements (country ad bans, mandatory disclaimers), mention them in the report. Search in both English and Vietnamese and across markets. Run date is today (UTC). Follow the full workflow: scout, then researcher and policy-checker in parallel, then reporter, then reviewer, then commit.
3. Outputs, and only these files:
   - Overwrite latest_report__tai-chinh-doi.md. Its first sentence must say this is the report of the agent team ('doi agent'), topic Finance ('Tai chinh'). Language, tone and format follow the skill report-format (Vietnamese, 'em'/'anh', Telegram HTML).
   - Append every candidate reviewed today, selected or rejected, to reported_programs__tai-chinh-doi.md. If the file does not exist, create it with the header '| Date | Product | Domain | Status | Note |' and '|------|---------|--------|--------|------|'. The Note column is in Vietnamese.
   - Do not modify files of other topics (latest_report__tai-chinh.md, reported_programs__tai-chinh.md, *ai*.md, ...).
   - If no program qualifies, still write the report listing the candidates reviewed and why each was rejected.
4. Network: this computer has normal internet access. WebFetch (and a browser, if available) may be used to read official pricing, affiliate and terms pages directly. Prefer primary sources over search snippets.
5. Commit: exactly one commit with message 'scout tai-chinh-doi YYYY-MM-DD', pushed directly to master (only the last commit is synced to Google Sheets). No branches, no pull requests. Before committing, run the check script from the skill report-format on the changed files and fix every ERROR. If the push is rejected because the remote moved: git pull --rebase origin master, re-run known_domains.py on the domains you added, then push again. If the push fails for a GitHub permission reason, stop and report it in the final summary.
6. Do not modify .github/workflows or anything under .claude.
7. Nobody is watching: never ask questions. Record any assumption in the final summary. If the reviewer still returns FAIL after 2 rounds, commit only when the check script reports no ERROR, and list the remaining issues in the final summary.
